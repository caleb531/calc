## `xor()`

Performs an XOR (exclusive OR) operation on two booleans.

```javascript
Calc.xor(false, false);
// false
Calc.xor(false, true);
// true
Calc.xor(true, false);
// true
Calc.xor(true, true);
// false
```

